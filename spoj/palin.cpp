#include <iostream>
#include <string>
using namespace std;
int main()
{
	ios::sync_with_stdio(false);cin.tie(0);
	long long int t;
	cin>>t;
	while(t--)
	{
		string s;
		cin>>s;
		int n;
		n = s.length();
		if(n%2==1)
		{
        
		}
	}

	return 0;
}